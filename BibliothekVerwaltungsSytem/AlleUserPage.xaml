<Page x:Class="BibliothekVerwaltungsSytem.AlleUserPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:BibliothekVerwaltungsSytem"
      mc:Ignorable="d"
      Title="AlleUserPage">

    <Grid Background="#F8F9FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tab-Navigation -->
        <Border Grid.Row="0" Background="White" BorderBrush="#BDC3C7" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Margin="20,0">

                <Button Content="USER HINZUFÜGEN"
                        Width="200" Height="50"
                        FontSize="14" FontWeight="SemiBold"
                        Background="#ECF0F1" Foreground="#7F8C8D"
                        BorderThickness="0" Margin="0,0,4,0"
                        Cursor="Hand"
                        Click="BtnTabUserHinzufuegen_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="ALLE USER"
                        Width="200" Height="50"
                        FontSize="14" FontWeight="Bold"
                        Background="#E74C3C" Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="BtnTabAlleUser_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

            </StackPanel>
        </Border>

        <!-- Suchbox -->
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="#E1E8ED"
                BorderThickness="0"
                CornerRadius="12"
                Margin="40,30,40,20"
                Padding="4">
            <Border.Effect>
                <DropShadowEffect Color="#E74C3C" Direction="270" ShadowDepth="0" BlurRadius="20" Opacity="0.15"/>
            </Border.Effect>
            <Grid>
                <TextBox x:Name="SearchTextBox"
                         FontSize="18" FontFamily="Segoe UI" FontWeight="Medium"
                         Foreground="#2C3E50" Background="Transparent"
                         BorderThickness="0" Padding="28,22,70,22"
                         VerticalContentAlignment="Center"
                         TextChanged="SearchTextBox_TextChanged"
                         CaretBrush="#E74C3C">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="BorderThickness" Value="0"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <TextBlock Text="Suche nach Benutzername, Name oder E-Mail..."
                           FontSize="18" FontFamily="Segoe UI" FontWeight="Medium"
                           Foreground="#95A5A6" Padding="28,22,70,22"
                           VerticalAlignment="Center" IsHitTestVisible="False">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsFocused, ElementName=SearchTextBox}" Value="False"/>
                                        <Condition Binding="{Binding Text.Length, ElementName=SearchTextBox}" Value="0"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <Border HorizontalAlignment="Right" VerticalAlignment="Center"
                        Width="48" Height="48" Margin="0,0,20,0"
                        Background="#ECF0F1" CornerRadius="24">
                    <TextBlock Text="🔍" FontSize="22" Foreground="#E74C3C"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsFocused, ElementName=SearchTextBox}" Value="True">
                                    <Setter Property="Background" Value="#E74C3C"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                </Border>
            </Grid>
        </Border>

        <!-- DataGrid -->
        <Border Grid.Row="2" Margin="40,0,40,40">
            <DataGrid x:Name="UserDataGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#ECF0F1"
                      BorderThickness="0"
                      Background="Transparent"
                      RowBackground="White"
                      AlternatingRowBackground="#FAFBFC"
                      HeadersVisibility="Column">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#E74C3C"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Padding" Value="16,12"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"       Binding="{Binding UserId}"   Width="0.5*"/>
                    <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="1.5*"/>
                    <DataGridTextColumn Header="Vorname"  Binding="{Binding Vorname}"  Width="1.5*"/>
                    <DataGridTextColumn Header="Nachname" Binding="{Binding Nachname}" Width="1.5*"/>
                    <DataGridTextColumn Header="E-Mail"   Binding="{Binding Email}"    Width="2*"/>
                    <DataGridTextColumn Header="Rolle"    Binding="{Binding Rolle}"    Width="1*"/>

                    <!-- Lösch-Spalte -->
                    <DataGridTemplateColumn Header="" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="🗑"
                                        FontSize="16"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        ToolTip="User löschen"
                                        Tag="{Binding UserId}"
                                        Click="BtnUserLoeschen_Click">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Foreground" Value="#E74C3C"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>

            </DataGrid>
        </Border>

    </Grid>
</Page>
